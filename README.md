# loop-engine-bootstrap-plugin

自举双 Loop plugin：一个 fleet 串联 Spec Loop（起草↔审）与 Impl Loop（实现↔审，复用 dev-dispatch spec mode）。

- 设计与 spec：`/data/vault/智元工作/工作记录/2026/07/03/loop-engine-自举双loop-plugin-设计/spec.md`
- 形态硬约束：一个 plugin、一个 fleet.yaml、loop 间靠 store 一跳 route，无胶水脚本；先串行。

**状态：scaffold，待 KIMI 按 spec 实现。**
