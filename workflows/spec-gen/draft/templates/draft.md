You are working in the git repository at:

{{workspace_repo}}

This is your workspace. It is an isolated clone: you may change anything inside it, and nothing outside it.

Reference library (read-only inspiration, not a todo list):
- Directory: {{ref_library_dir}}
- Index/abstracts: {{ref_library_index}}

Idea seed:
- idea_id: {{idea_id}}
- prior feedback summary: {{feedback}}
- prior feedback file: {{feedback_file?}}

## MANDATORY Step 0: ROADMAP.md

**This is the most important step. You MUST complete it before writing any specs.**

1. Check if `{{workspace_repo}}/docs/ROADMAP.md` exists.
   - If YES: Read ROADMAP.md. It lists all existing specs. You only need to read the most recently added specs (check `git log --oneline --diff-filter=A -n 20 -- docs/specs/`).
   - If NO: This is the first run. Read ALL spec files in `docs/specs/` to generate the initial ROADMAP.md. This is a one-time cost.

2. **You MUST create or update `{{workspace_repo}}/docs/ROADMAP.md` BEFORE writing any new specs.** Format:
   ```
   # Spec Roadmap
   > Auto-generated by batch drafter. Lists all implemented specs to avoid duplication.

   ## Implemented
   | SPEC | Title | Summary |
   |------|-------|---------|
   | SPEC-001 | ... | One-line summary |

   ## This Batch (in progress)
   | SPEC | Title | Summary |
   |------|-------|---------|
   ```

3. After writing ROADMAP.md, commit it immediately:
   ```
   git -C {{workspace_repo}} add docs/ROADMAP.md && git -C {{workspace_repo}} commit -m "chore: update spec roadmap"
   ```

## Step 1: Generate Specs

Based on existing specs (from ROADMAP.md), identify 3-5 genuinely new, non-overlapping, independently implementable features.

Write each spec at `{{workspace_repo}}/docs/specs/SPEC-<unique>.md`.

## Step 2: Update ROADMAP.md Again

After writing specs, update `{{workspace_repo}}/docs/ROADMAP.md` to add the new specs to the "This Batch" section.

## Step 3: Final Commit

Commit all changes:
```
git -C {{workspace_repo}} add docs/ROADMAP.md docs/specs/ && git -C {{workspace_repo}} commit -m "chore: batch draft — add specs and update roadmap"
```

## Step 4: Verification

Before returning, verify:
```
ls {{workspace_repo}}/docs/ROADMAP.md
```
If this file does NOT exist, you have FAILED. Create it immediately.

## Step 5: Return JSON Envelope

Return one enqueue effect per spec:
```json
{
  "result": "batch-drafted SPEC-XXX, SPEC-YYY, ...",
  "effects": [
    {
      "op": "enqueue",
      "queue": "spec-pr",
      "task": {
        "id": "spec-pr-SPEC-XXX",
        "status": "ready",
        "spec_id": "SPEC-XXX",
        "spec_file": "/absolute/path/to/{{workspace_repo}}/docs/specs/SPEC-XXX.md"
      }
    }
  ]
}
```

Use absolute paths for spec_file. Produce 3-5 specs unless the codebase is very small or already well-covered.

If prior feedback is not "(none)", read the feedback file and address every point. Each spec should be self-contained and can be implemented in parallel with others.
